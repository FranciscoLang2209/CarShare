# AWS Instance Configuration
AWS_PUBLIC_IP=107.22.158.177

# API Configuration
# For server-side (Next.js on AWS): use localhost (fast)
API_URL=http://localhost:3001

# For client-side (browser): use public IP (now that port 3001 is open)
NEXT_PUBLIC_API_URL=http://107.22.158.177:3001